Git commands

git config --global user.name "John Doe"

 git config --global user.email gabjimenezv@gmail.com

git clone https:// 

git status

git add archivo (. add all files or -A) 

git commit -m “mensaje” // no sincroniza con git.com

git push  // publica en github.com 

 git pull // actualiza en el directorio  

ESC :wq para salir del editor

git remote add origin https://

git diff  // para ver cuales cambios no han sido enviados a stage store 

git diff —stage  // cuales archivos en stage son diferentes del ultimo commit

git diff —color-word // cuales palabras difieren muestra en colores

git diff —word-diff // exactamente igual XD

git diff —stat //muestra de manera simplificada las diferencias

git log // muestra el historial de commits

git los —oneline // muestra un resumen del log 
